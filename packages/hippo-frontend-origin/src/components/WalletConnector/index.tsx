import Button from 'components/Button';

const WalletConnector: React.FC = (props: any) => {
  return (
    <Button
      className="min-w-[156px] border-2 shadow-md paragraph h-full bg-secondary header5 bold"
      onClick={() => {}}>
      Connect To Wallet
    </Button>
  );
};

export default WalletConnector;
